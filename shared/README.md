# Shared Module Placeholder

This folder is reserved for shared media log logic.

Move these ideas here first:

- entry types
- validation
- ISO week logic
- week bounds
- JSON import and export shape
- type labels

The current source of truth is split between `extension/popup.js` and `scripts/lib/`.
